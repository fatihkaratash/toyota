package com.toyota.mainapp.model;

/**
 * Enumeration representing different statuses of a rate.
 */
public enum RateStatus {
    ACTIVE,
    INACTIVE,
    SUSPENDED,
    INVALID,
    PENDING,
    ERROR;
}
